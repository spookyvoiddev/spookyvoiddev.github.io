# Social media handles surfaced via the jekyll-socials plugin and the search
# modal (when `socials_in_search: true` in _config.yml).
#
# This site currently surfaces no public socials. Re-add entries here when
# ready (see https://github.com/george-gca/jekyll-socials for the supported
# keys: linkedin_username, github_username, email, etc.).
rss_icon: true
